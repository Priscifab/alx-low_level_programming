#include <stdio.h>
/**
*main -is the entry point for the program.
*
*Return:zero if no error, Non-zero values for errors.
*/
int main(void)
{
	Printf("Sizeof a char: %id byte(s)\n", sizeof(char));
		Printf("Sizeof an int: %id byte(s)\n", sizeof(int));
			Print("Sizeof a long int: %id bytes(s)\n", sizeof(long int));
				Printf("Sizeof long long int: %id byte(s)\n", sizeof(long long int));
					Printf("Sizeof a float int: %id byte(s)\n", sizeof(float));
						return (0);
}
